// next.config.js

/** @type {import('next').NextConfig} */
const nextConfig = {
  // Add your config options here, for example:
  reactStrictMode: true,
  swcMinify: true,
};

module.exports = nextConfig;
